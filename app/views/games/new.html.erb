<!DOCTYPE html>
<html>
<head>
  <title>Nouvelle Partie - Jeu de Yams</title>
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />
  <script
    src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"
  ></script>
  <style>
    .centered-content {
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      background-color: #f8f9fa; /* Couleur de fond clair */
    }
    .player-input {
      max-width: 400px;
    }
  </style>
</head>
<body>
  <div class="container centered-content">
    <!-- Header -->
    <h1 class="text-primary mb-4">Nouvelle Partie</h1>
    <p class="text-muted text-center mb-4">Veuillez entrer entre 2 et 4 joueurs pour démarrer la partie.</p>

    <!-- Formulaire -->
    <%= form_with url: games_path, method: :post, local: true, class: "player-input" do %>
      <% @players.each_with_index do |player, index| %>
        <div class="mb-3">
          <%= label_tag "players[#{index}]", "Nom du joueur #{index + 1}", class: "form-label" %>
          <%= text_field_tag "players[#{index}]", nil, placeholder: "Nom ou pseudo", required: false, class: "form-control" %>
        </div>
      <% end %>

      <div class="text-center">
        <%= submit_tag "Commencer la partie", class: "btn btn-success btn-lg" %>
      </div>
    <% end %>
  </div>
</body>
</html>
